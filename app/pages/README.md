# Deprecated

Do not add Python files here. Streamlit auto-discovers `pages/` and runs scripts directly, which caused blank screens because only `render()` was defined.

All UI modules are in `app/views/` and routed from `app/main.py`.
